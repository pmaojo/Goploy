# Database migrations

This project currently ships without SQL migrations. The directory is kept to satisfy
build and packaging steps that expect `/migrations` to exist, and should be populated
with migration scripts as soon as they become available.
